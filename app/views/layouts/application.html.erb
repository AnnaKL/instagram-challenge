<!DOCTYPE html>
<html>
<head>
  <title>InstagramChallenge</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
 <div class='user'>
  <% if user_signed_in? %>
    <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'links' %>
    <h2>Welcome <%= current_user.email %></h2>
  <% else %>
    <span><%= link_to "Sign in", new_user_session_path, class: 'links' %></span>
    <span><%= link_to "Sign up", new_user_registration_path, class: 'links' %></span></br>
    <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook), class: 'links' %><br>
  <% end %>
  <%= yield %>
  </div>
</div>
</body>
</html>
